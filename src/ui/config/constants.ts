/**
 * Application-wide constants
 */

// Pagination
export const DEFAULT_PAGE_LIMIT = 50;

// Query History
export const QUERY_HISTORY_STORAGE_KEY = 'd1_query_history';
export const MAX_QUERY_HISTORY_ITEMS = 100;

// Export filenames
export const DEFAULT_EXPORT_FILENAME = {
  CSV: 'export.csv',
  JSON: 'export.json',
  QUERY_CSV: 'query_results.csv',
  QUERY_JSON: 'query_results.json',
};
